# ─────────────────────────────────────────────────────────────────────────────
#  WoundAI — Python Dependencies  (pinned to versions verified in venv)
#  Python       : 3.10.11
#  GPU backend  : tensorflow-directml-plugin 0.4.0 (Windows DirectX 12 GPU)
#
#  Install into venv:
#      python -m pip install -r Requirements.txt
#  NOTE: always use  python -m pip  — the pip.exe launcher is broken
#        when the venv folder has been moved/renamed.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Deep Learning ────────────────────────────────────────────────────────
# GPU is active via DirectML plugin (Windows DirectX 12 — no CUDA needed).
# DO NOT upgrade tensorflow without also verifying the DirectML plugin version.
tensorflow==2.10.1
tensorflow-directml-plugin==0.4.0.dev230202   # GPU on Windows (AMD/Intel/NVIDIA)

# ── Web API  (app.py) ─────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22
pydantic==2.12.5

# ── Image Processing ──────────────────────────────────────────────────────────
opencv-python==4.11.0.86
Pillow==12.1.1
ImageHash==4.3.2           # perceptual hash deduplication (Preprocess.py)

# ── Numerical / ML ────────────────────────────────────────────────────────────
# numpy MUST stay < 1.24 — TensorFlow 2.10 is incompatible with numpy >= 1.24
numpy==1.23.5
scikit-learn==1.7.2

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.10.8
seaborn==0.13.2

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.67.3               # progress bars (Preprocess.py)

# ─────────────────────────────────────────────────────────────────────────────
#  GPU TRAINING NOTES (DirectML on Windows)
# ─────────────────────────────────────────────────────────────────────────────
#  • GPU is detected automatically via DirectML — no CUDA/cuDNN required
#  • mixed_float16 is DISABLED for DirectML (unsupported ops crash DirectML)
#  • Only RandomFlip augmentation is used — Zoom/Contrast/Brightness all
#    trigger unsupported DirectML ops (ImageProjectiveTransformV3, etc.)
#  • Run training:
#      python Train_cpu.py --data_dir ./datasets --output_dir ./results
#  • Force CPU-only (debugging):
#      python Train_cpu.py --data_dir ./datasets --output_dir ./results --no_gpu
# ─────────────────────────────────────────────────────────────────────────────
